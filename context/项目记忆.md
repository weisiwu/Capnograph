<!-- context-seed:start -->
# 项目记忆

## Context Seed 初始化

- 初始化来源: 自动目录检查。
- 主要定位文件: `context/实体标识映射.md`。
- 实体补充上下文目录: `context/文档/实体/`。
- 生成的技能: `skills/context-aware-dev/SKILL.md`。
- 手写备注应保留在 `context-seed` 托管区块之外。

## 后续备注

- 当项目领域边界更清晰后，在 `context/文档/` 下补充领域上下文文档。
- 当 ID、文件名、资产或所属模块移动时，同步更新实体映射。
- AI Agent 自动化场景优先使用 `prepare --json` 生成计划，再用 `apply --plan-file` 写入。

## 最近提交收尾

- 提交: `f5c44d4` chore: configure omp codex models
- 作者: weisiwu
- 时间: 2026-06-15T00:45:27+08:00
- 变更文件数: 2
- 变更类别: 配置 1, 上下文 1

### 变更文件
- `M` `.omp/config.yml` (配置)
- `M` `context/项目记忆.md` (上下文)
<!-- context-seed:end -->
## 手写备注：项目 Wiki 与代码图

- 2026-07-18：在分支 `monorepo_v1`（提交 `edfd024`）的干净 worktree 上创建本地 GitNexus 索引和中文项目 Wiki；索引位于 `.gitnexus/`，包含 7,619 个节点、10,822 条边和 260 条执行流，Wiki 入口为 `.gitnexus/wiki/index.html`，初始共 11 个 Markdown 页面。
- 2026-07-19：审计并补充项目 Wiki，新增专题入口、业务领域与端到端流程、技术框架与数据契约、审核与评审指南 4 页，Wiki 共 15 页；专题事实以当前分支源码、配置、测试、`context/` 与 GitNexus 执行流为证据，并把报警区间、记录结束时间、临时 chunk 参数、Room schema、敏感权限/诊断字段和测试缺口列为待关闭审核项。
- 2026-07-19：在本地 GitNexus Wiki 之上建立受 Git 管理的 Material for MkDocs 正式知识站：人工业务、架构、审核手册迁入 `docs/`，11 个 GitNexus 自动页面通过 `scripts/sync_gitnexus_wiki.py` 同步到 `docs/generated/gitnexus/`；`site/` 作为离线静态构建产物由 Git 忽略。
- 2026-07-19：第二轮优化正式知识站的样式、内容与写作：新增五分钟图解导览，补齐设备连接、实时数据、记录持久化、对象关系、改动传播和审核门禁等 18 张 Mermaid 图；确认图表由 Material 的封闭 Shadow DOM 渲染，避免重复初始化，并统一首页导览、页面摘要、图注、移动端宽图滚动和写作约定。
- 2026-07-19：第三轮优化把正式知识站扩展为 24 页：长业务/架构/审核页拆成总览与 7 个专题，新增 BLE/写入/恢复故障路径和患者数据生命周期图；图表具备缩放、重置、全屏、打印/保存与可见文字摘要，中文搜索接入 jieba 项目词典，GitHub Actions 新增同步、结构与严格构建门禁。
- 2026-07-19：浏览器复验确认首页、图解导览、架构、审核、故障路径和患者数据页在 1280×720 无整页横向溢出；390×844 下图表工具条仅在自身内滚动，放大 110% 与重置 100% 实际可操作；“图解导览”搜索首位已修正为五分钟导览。
- 2026-07-19：建立 FigJam 审阅板 `https://www.figma.com/board/C70CG1Stzl2D7lHLuOuSyA`，按左到右顺序放置 7 张本轮修复后截图，截图下方记录问题、修复和验收证据。
- 2026-07-19：Wiki 业务知识从单一应用流程改为两层导航：新增“行业与业务背景”，用外部权威来源解释二氧化碳描记行业、参数、场景、主流/旁流和标准监管边界；原业务页更名为“CapnoEasy 应用业务与端到端流程”。面向读者的“GitNexus 自动文档”分类同时更名为“代码模块参考”，保留页面内部的生成来源标识。
- 2026-07-19：“代码模块参考”从仓库目录名平铺改为四组读者导向导航：仓库全景、移动应用、构建与交付、项目知识与 AI；`Root`、`apps`、`context — docs` 等原始目录名仅保留为生成页来源，左侧栏改用可理解的中文任务名。
- 2026-07-19：“代码模块参考”分组增加条件式手风琴交互：至少有两个直接子项时才可展开/收起并与同级分组互斥，单子项分组不折叠；当前页所属分组在首次加载时优先展开。大项使用带左侧强调线的色块标题，子项改为缩进、较小字号的轻量列表。
- 2026-07-19：恢复“行业与业务背景”和“CapnoEasy 应用业务”页的左侧导航，业务知识三页统一为左侧展示整体知识结构、右侧展示当前页章节目录。
- 2026-07-18：创建基于当前分支受版本控制文件快照的 Graphify 索引 `graphify-out/graph.json`，最终验证为 5,998 个节点、6,915 条边，未混入 `.gradle`、`build`、`.gitnexus` 或 `graphify-out` 生成内容。
- GitNexus 与 Graphify 产物为本地生成目录并由 Git 忽略；可复现命令见 `README.md` 的 “Local Project Wiki And Code Graphs”。

## 手写备注：CapnoEasy 十套界面重设计

- 2026-07-19：本机 OpenDesign 完成 `design/capnoeasy-ten-redesign-directions.html`；产物是单文件响应式设计板，含 10 个方向、20 个 390×844 手机状态，且保留临床监护、告警处置、记录、报告与无障碍等真实产品约束。
- 该产物已通过浏览器结构与可视验收：10 个方向索引、20 个手机画布、10 个高优告警状态，无屏内或子容器溢出；未更改应用源码。

## 手写备注：Oh My Pi 项目环境

- 2026-06-15：新增项目级 Oh My Pi 配置目录 `.omp/`。
- OMP skills：`capno-context`、`capno-android`、`capno-ios-parity`、`capno-packaging`、`capno-packflow`。
- OMP tools：`capno_context_lookup`（实体/上下文定位）、`capno_package`（root 打包脚本预览/执行）、`capno_packflow_bot_command`（飞书原始文本口令路由）、`capno_packflow_agent`（Packflow 后台 AI/机器人打包+产物扫描+飞书通知）、`capno_packflow_query`（Packflow 配置/最新/历史/详情查询）、`capno_packflow`（本地直接打包+产物扫描+飞书通知）、`capno_feishu_send`（飞书消息推送）。
- `.omp/config.yml` 将 `.omp/skills` 和根目录 `skills` 加入 `skills.customDirectories`，使已有 `skills/context-aware-dev` 可被 OMP 发现。
- `.omp/config.yml` 启用 `deepseek/deepseek-v4-pro` 作为 default 模型，`openai-codex/gpt-5.5` 作为 slow/plan 模型，并用 `deepseek/deepseek-v4-flash` 作为 smol 模型。
- 2026-06-14：根目录新增 `架构问题分析.md`，总结 Android/iOS 架构风险、证据路径和治理顺序；未修改业务行为。
- 2026-06-16：Android 错误上报通过 `ErrorReporter` 统一接入 Bugly CrashReport，Application 初始化，BaseActivity 同步页面上下文，Main/History/PDF/BLE/备份/图片/报警音等已捕获异常按非致命异常上报；metadata 避免直接携带患者字段。iOS 工程当前没有 Firebase Crashlytics/Sentry/Bugly 等真实上报 SDK 依赖，未在本次强行接入。
- 2026-06-17：新增 OMP PackFlow 工具链（`.omp/tools/capno-packflow.js`）和技能说明（`.omp/skills/capno-packflow/SKILL.md`），支持 `capno_packflow` 一键打包 + 产物扫描，并通过 Feishu webhook 通知；提供 `capno_feishu_send` 做人工推送。
- 2026-06-23：本机 Packflow 后台 `http://localhost:3001` 已接入项目 `CapnoGraph`，登记 `Android Release APK` 和 `iOS Release Archive` 两条 release 构建配置；Android 走 `docker compose run --rm android-builder scripts/package.sh --target android --variant release -- --no-daemon`，iOS 走 `scripts/package.sh --target ios --variant release`；当前分支构建显式传 `branch=monorepo_v1`。
- 2026-06-23：新增 Packflow `Android Debug APK` 快速配置，清空 install command 后，warm build `6bb1f453-02ab-4b2f-8599-0c6565924701` 通过 Packflow 在 `29.043s` 内产出 `apps/android/app/build/outputs/apk/debug/app-debug.apk`；`Android Release APK` 仍在 `:app:minifyReleaseWithR8` 阶段失败，不能作为已达成 release 目标。
- 2026-06-23：新增 OMP `capno_packflow_agent`，把 Packflow `Android Debug APK` 配置改造成 AI/机器人可调用能力；端到端验证 build `f74cdf65-b3c6-4ad0-9ed4-2903f8ebe144` 用时 `25.090s`，产物 `app-debug.apk` SHA-256 为 `0705f73d1904c8560a3253ae53cefe692248f69727fdbde182675f9f0aff1cad`，并成功通知飞书机器人 `CapnoGraph OMP Bot`。
- 2026-06-23：新增 OMP `capno_packflow_bot_command` 与 `capno_packflow_query`；飞书文本 `打包` / `打 APK` / `capno_packflow_agent` 会映射到 debug APK 打包，`打包状态` / `打包历史` / `打包详情 <buildId>` / `打包配置` 映射到 Packflow SQLite 查询，`打 release 包` 会因 release R8 不稳定被拦截。
- 2026-06-24：确认 Android debug APK `105690613` bytes 超过飞书文件上传限制；复验 build `df3a3a14-61db-48ba-811e-c87383e3566a` 成功，用时 `26.362s`，artifact `59d86d7d-a5c5-44b6-8a35-92e9991e80d3`，SHA-256 `15ab3ba39dff9b66aca24a40150e4a731eb8e67fab69bd41b6cb31b8dbedf730`，Feishu webhook 返回 `StatusCode=0`；监控 harness 曾因 `signal=null` 报错，已记录待后续修复。
- 2026-06-24：修正飞书下载交付策略：大 APK 只发送 Packflow 公开 token 链接 `/api/public/artifacts/<artifactId>/download?token=<token>`，通知前必须具备公网 Packflow 地址；可通过 `CAPNOGRAPH_PACKFLOW_PUBLIC_BASE_URL` / `PACKFLOW_PUBLIC_BASE_URL` 显式配置，或从本机 `~/.cloudflared/packflow-baoganai.yml` 自动发现 `https://packflow.baoganai.com`，缺少公网地址时阻止发送 `localhost` 链接；Android APK 构建产物名改为追加版本和时间，如 `app-debug-v1.2-20260624-153012.apk`。
