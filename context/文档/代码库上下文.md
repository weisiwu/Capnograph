<!-- context-seed:start -->
# 代码库上下文

本文件只保留仓库级总览。具体配置、符号和领域信息按大写短 ID 拆分到 `context/文档/实体/`，由 `context/实体标识映射.md` 定位。

## 源码与配置概览

- 顶层版本控制目录: `.omp`, `apps`, `context`, `docker`, `scripts`, `skills`
- 主要扩展名: `.md` (515), `.kt` (53), `.png` (45), `.svg` (42), `.json` (29), `.swift` (16), `.xml` (13), `<none>` (5), `.wav` (4), `.kts` (4), `.jpg` (3), `.pro` (3)
- 实体上下文数量: 80

## 项目 Wiki 与代码图

- 正式项目知识站: `mkdocs.yml` + `docs/`；Material for MkDocs 提供中文导航、搜索、Mermaid 和离线静态构建，人工知识与 `docs/generated/gitnexus/` 自动内容分区维护。
- 图解入口: `docs/guide/visual-overview.md`；人工手册共维护 18 张可追踪 Mermaid 图，覆盖产品边界、业务主线、BLE 初始化、实时数据、记录持久化、数据关系、改动传播、审核与文档维护流程。
- GitNexus: `.gitnexus/`；基于分支 `monorepo_v1`、提交 `edfd024` 的干净 worktree，7,619 个节点、10,822 条边、260 条执行流。
- GitNexus 本地源 Wiki: `.gitnexus/wiki/index.html`；中文，15 个 Markdown 页面；11 个自动页面由 `scripts/sync_gitnexus_wiki.py` 同步进入正式文档站。
- Graphify: `graphify-out/graph.json`；基于当前分支受版本控制文件快照，5,998 个节点、6,915 条边。
- `.gitnexus/`、`graphify-out/` 和 `site/` 由 Git 忽略；正式文档源 `docs/` 受版本控制，刷新和构建命令见 `README.md`。

## 上下文加载流程

- 先在 `context/实体标识映射.md` 搜索实体名、文件路径、领域词或 ID。
- 记录命中的唯一 ID。
- 只加载该行 `补充上下文` 指向的实体文档。
- 需要确认行为时，再打开实体文档中的源码位置。

## 工作指引

- 修改前先通过 `context/实体标识映射.md` 解析名称。
- AI Agent 调用场景优先使用 `python -m context_seed prepare . --operation init --json`，按 `ai_tasks` 顺序逐个补充实体上下文，审阅计划后再 `apply`。
- 行为变更前，将实体上下文与源码文件交叉验证。
- 修改后只更新相关实体上下文和映射行，并按需刷新一次可选图索引。

## 最近提交影响

- 提交 `f5c44d4` 影响类别: 配置 1, 上下文 1
- 代码/配置/技能变更: `.omp/config.yml`
- 上下文/文档变更: `context/项目记忆.md`
<!-- context-seed:end -->
